"use strict";
import { capitalizeString } from "string_functions"
// string_functions is a node module in this example 
// if it was a file in the same directory as this file we must use "./"
capitalizeString("hello!");